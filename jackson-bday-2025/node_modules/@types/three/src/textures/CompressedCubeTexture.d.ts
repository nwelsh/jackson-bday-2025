import { CompressedPixelFormat, TextureDataType } from "three/src/constants";
import { CompressedTexture } from "three/src/textures/CompressedTexture";

export class CompressedCubeTexture extends CompressedTexture {
    readonly isCompressedCubeTexture: true;
    readonly isCubeTexture: true;

    constructor(
        images: Array<{ width: number; height: number }>,
        format?: CompressedPixelFormat,
        type?: TextureDataType,
    );
}
