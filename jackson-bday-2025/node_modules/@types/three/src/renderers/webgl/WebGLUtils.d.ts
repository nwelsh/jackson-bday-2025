import { CompressedPixelFormat, PixelFormat, TextureDataType } from "three/src/constants";
import { WebGLExtensions } from "three/src/renderers/webgl/WebGLExtensions";

export class WebGLUtils {
    constructor(
        gl: WebGLRenderingContext | WebGL2RenderingContext,
        extensions: WebGLExtensions,
    );

    convert(p: PixelFormat | CompressedPixelFormat | TextureDataType, colorSpace?: string): number | null;
}
